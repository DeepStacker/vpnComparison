---
import SeoLayout from "@/layouts/SeoLayout.astro";
import Section from "@/components/ui/Section.astro";
import Container from "@/components/ui/Container.astro";
import Heading from "@/components/ui/Heading.astro";
import Text from "@/components/ui/Text.astro";
import Badge from "@/components/ui/Badge.astro";
import Icon from "@/components/ui/Icon.astro";
import SearchBar from "@/components/search/SearchBar.astro";
import { buildTitle, normalizeSeoMeta, canonicalUrl } from "@/lib/seo/meta";
import type { Breadcrumb } from "@/types/site";

const seo = normalizeSeoMeta({
  title: buildTitle("Search — Find VPN Reviews, Guides & Comparisons"),
  description: "Search FreeVPN4USA for VPN reviews, comparisons, guides, and FAQs.",
  canonical: canonicalUrl("/search/"),
});

const breadcrumbs: Breadcrumb[] = [
  { label: "Search", href: "/search/" },
];
---

<SeoLayout seo={seo} breadcrumbs={breadcrumbs}>
  <Section class="relative overflow-hidden pb-20 pt-24 md:pb-28 md:pt-32">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-b from-primary/5 via-transparent to-transparent"></div>
      <div class="absolute left-1/2 top-0 h-[500px] w-[1000px] -translate-x-1/2 bg-primary/5 blur-[120px]"></div>
    </div>
    <Container>
      <div class="mx-auto max-w-3xl text-center">
        <Badge tone="primary" class="mb-6">Global Search</Badge>
        <Heading level={1} class="text-4xl font-black tracking-tight md:text-6xl">
          Search <span class="text-primary">FreeVPN4USA</span>
        </Heading>
        <Text size="xl" tone="muted" class="mt-6 max-w-2xl mx-auto leading-relaxed">
          Find VPN reviews, comparisons, guides, and FAQs across our entire database.
        </Text>
        <div class="mt-10">
          <SearchBar />
        </div>
        <div class="mt-8 flex items-center justify-center gap-4 text-sm font-bold text-muted-foreground uppercase tracking-widest">
          <Icon name="info" size={16} />
          <span>Requires JavaScript for full functionality</span>
        </div>
      </div>
    </Container>
  </Section>
</SeoLayout>
